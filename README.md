svelte.js + tornado.py
---------------------

Minimal template serving a Svelte app from a Tornado webserver.

`npm install`
`pip install -r requirements.txt`

Then `npm dev` and visit `http://localhost:8888`

